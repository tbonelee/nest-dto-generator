type GenerateCommandOptions = {
  src: string;
  out: string;
};

export const generateCommand = async (options: GenerateCommandOptions) => {
  const { src, out } = options;

  // 1. Analyze source files

  // 2. Structuring output types

  // 3. Write to files
};
